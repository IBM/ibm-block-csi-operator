# Adds namespace to all resources.
namespace: default


namePrefix: ibm-block-csi-operator-

# Labels to add to all resources and selectors.
#commonLabels:
#  someName: someValue

bases:
- ../crd
- ../rbac
- ../manager

# the following config is for teaching kustomize how to do var substitution
vars:
