kind: DaemonSet
apiVersion: apps/v1
metadata:
  name: ibm-iscsi-agent
  namespace: kube-system
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ibm-iscsi-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ibm-iscsi-agent
    spec:
      hostNetwork: true
      containers:
      - name: iscsi-agent
        securityContext:
          privileged: true
          capabilities:
            add: ["SYS_ADMIN"]
          allowPrivilegeEscalation: true
        image: stg-artifactory.haifa.ibm.com:5030/ibm-iscsi-agent:0.0.1
        imagePullPolicy: IfNotPresent
        env:
        - name: ADDRESS
          value: :10086
        ports:
        - name: iscsi
          containerPort: 10086
        volumeMounts:
        - mountPath: /lib/modules
          name: lib-modules
        - mountPath: /sys
          name: sysfs
        - mountPath: /dev
          name: dev
      volumes:
      - name: lib-modules
        hostPath:
          path: /lib/modules
          type: Directory
      - name: sysfs
        hostPath:
          path: /sys
          type: Directory
      - name: dev
        hostPath:
          path: /dev
          type: Directory
