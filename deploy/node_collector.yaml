apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: ibm-node-collector
  namespace: kube-system
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ibm-node-collector
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ibm-node-collector
    spec:
      serviceAccountName: ibm-node-collector
      initContainers:
      - name: node-collector
        image: stg-artifactory.haifa.ibm.com:5030/ibm-node-collector:0.0.3
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - name: iscsi
          mountPath: /etc/iscsi
        - name: fc
          mountPath: /sys/class
        env:
        - name: WATCH_NAMESPACE
          value: ""
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
      volumes:
      - name: iscsi
        hostPath:
          path: /etc/iscsi
          type: DirectoryOrCreate
      - name: fc
        hostPath:
          path: /sys/class
          type: Directory
      containers:
      - name: sleep
        image: stg-artifactory.haifa.ibm.com:5030/ibm-node-collector:0.0.3
        imagePullPolicy: IfNotPresent
        command:
        - tail
        - -f
        - /dev/null
